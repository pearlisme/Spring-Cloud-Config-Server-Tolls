


server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/pearlisme/spring-cloudconfig-tolls
          # username
          # password
          search-paths:
            - 'station*'
          repos:
            perf:
              pattern:
                - "*/perf"
              uri: https://github.com/pearlisme/spring-cloudconfig-tolls-perf
              search-paths:
                - 'station*'

